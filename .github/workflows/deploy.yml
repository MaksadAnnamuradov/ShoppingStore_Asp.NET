name: "deploy"

on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: checkout repo
        uses: actions/checkout@v2
        with: 
          fetch-depth: 100

      - name: build
        run: |
          docker-compose up --build -d
