{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "DATABASE_URL": "server=localhost; port=5432; database=shopstore; user id=postgres; password=password",
  //docker run -d -p 5432:5432 --name shopstore -e POSTGRES_PASSWORD=password --restart always shopstore

  "IdentityConnection": "server=localhost; port=5434; database=identity; user id=postgres; password=password"
  //docker run -d -p 5434:5432 --name identity -e POSTGRES_PASSWORD=password --restart always identity
}
