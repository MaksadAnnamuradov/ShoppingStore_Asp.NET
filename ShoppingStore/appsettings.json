{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "DATABASE_URL": "server=localhost; port=5432; database=postgres; user id=postgres; password=admin",
  //docker run -d -p 5432:5432 --name pg -e POSTGRES_PASSWORD=admin --restart always postgres

  "IdentityConnection": "server=localhost; port=5434; database=postgres; user id=postgres; password=password"
  //docker run -d -p 5434:5432 --name Identity -e POSTGRES_PASSWORD=password --restart always postgres
}
