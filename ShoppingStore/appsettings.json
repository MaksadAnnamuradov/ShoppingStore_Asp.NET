{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "DATABASE_URL": "server=shopdb; port=5432; database=postgres; user id=shop_user; password=password",
  //docker run -d -p 5432:5432 --name shopstore -e POSTGRES_PASSWORD=password --restart always shopstore

  "IdentityConnection": "server=identitydb; port=5432; database=postgres; user id=identity_user; password=password"
  //docker run -d -p 5434:5432 --name identity -e POSTGRES_PASSWORD=password --restart always identity
}
